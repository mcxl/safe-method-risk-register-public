# Private Golden Fixtures Omitted

The private repository uses golden Office masters, canonical project briefs and
full document-set regression fixtures for acceptance testing. Those files are
not included in this public mirror because they may contain licensed,
client-identifying or consultant-reviewed benchmark material.

The implementation code remains visible, but private acceptance gates that read
`fixtures/golden/` require the private repository assets.
