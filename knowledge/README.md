# Private Knowledge Data Omitted

The private repository includes vetted WHS knowledge-source JSON used by the
generation and validation pipeline. That data is intentionally omitted from this
public code mirror.

The public mirror shows the implementation boundaries for knowledge loading,
retrieval, validation and rendering, but it is not a standalone WHS source of
truth and cannot produce issue-ready outputs without the private governed
knowledge assets and consultant sign-off.
