# Outputs

Generated outputs are intentionally not included in this public mirror.

Private workflow runs keep scratch output under `outputs/tmp/` or
`outputs/local/`. Controlled issued outputs, when deliberately created in the
private repository, require deterministic validation, preflight evidence,
verifier evidence and consultant sign-off.
