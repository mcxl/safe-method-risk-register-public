# Prompt Specification

Phase 2/4 owns `30-prompt-system.md`. This directory contains LLM judgment instructions
only. Deterministic rules belong in `rules/`; vetted safety-critical content belongs in
`knowledge/`.
